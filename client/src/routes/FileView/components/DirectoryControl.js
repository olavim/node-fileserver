import React from 'react';

export default ({
	
}) => {
	return (
		<div className="control">
			<span className="new-file" data-toggle="tooltip" title="Upload a file" />
			<span className="new-folder" data-toggle="tooltip" title="Create a new folder" />
		</div>
	)
}